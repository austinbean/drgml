# map and merge
#=

- Take a large matrix with N rows, M << N Columns.
- This is data in long form: multiple rows for each unique record
- Return a list with N' rows and M' columns.
- One row for each record.  
- M' >> M should be 0/1 for each entry, depending on whether a feature is present or not.  

=#

function StEnum(A::Array, b::Int)
# this probably isnt necessary anymore.  
return nothing 

end 